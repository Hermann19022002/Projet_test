# Requirements MINIMAUX pour le projet Python - Version 1
# TDs 3 à 5
# Compatible Python 3.10+ (testé sur Python 3.13)
# Cette version utilise uniquement des modules de la bibliothèque standard sauf pour les APIs

# Récupération de données
praw>=7.7.0              # Reddit API
xmltodict>=0.13.0        # Parsing XML pour Arxiv

# Note : pandas et numpy ne sont PAS requis pour cette version
# Le module csv standard de Python est utilisé à la place
# Si vous souhaitez utiliser pandas, installez-le séparément :
# pip install pandas

# Optionnel mais recommandé
requests>=2.28.0         # Requêtes HTTP (déjà installé avec praw)
